<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
	<!-- NanoKVM is addressed over cleartext HTTP (http://<LAN-IP>). Android blocks
	     cleartext traffic by default from API 28 on — so allow it here, otherwise the
	     NanoKVM connection fails with "Connection Failure" (the ServerRemote API itself uses
	     HTTPS and would not be affected). -->
	<application android:allowBackup="true" android:icon="@mipmap/appicon" android:roundIcon="@mipmap/appicon_round" android:supportsRtl="true" android:usesCleartextTraffic="true"></application>
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.INTERNET" />
</manifest>